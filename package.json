{
    "name": "cf-worker-wasm",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
        "start": "node --experimental-vm-modules serve.js",
        "publish": "wrangler publish"
    },
    "devDependencies": {
        "esbuild": "^0.15.2",
        "miniflare": "^2.6.0",
        "wrangler": "^2.0.25"
    }
}
